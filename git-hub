# git clone ссылка проекта

# python -m venv .venv #вертуальное окружение

# git init - инициализация проекта (отслеживание)

# git add . - отправить все фал

# git commit -m 'коммент изменений' - снимок экрана

# git branch -M main - выбор ветки

# git remote add origin ссылка - подключение к репозиторию

# git push -u origin main - залить все изменения в главную ветку


# git config --list = информация git


# ================================================================================


# git add . - отправить все фал

# git commit -m 'коммент изменений' - снимок экрана

# git push -u origin main - залить все изменения в главную ветку

# ========================================================================

# git checkout -- .  или имя файла # отмена изменений

# git reset . или имя файла # чтоб извлечь из временных файлов

# git reset --hard HEAD^1 жесткий сброс на один комит

# git reset --soft HEAD^1 - МГЯКИЙ СБРОС

# git branch название ветки - создание ветки

# git checkout название ветки - чтоб перейти на ветку

# git checkout -b название ветки - создать и перейти на ветку

# git merge название ветки - слияние ветки





# Ты можешь делиться результатами с друзьями при помощи export tree и import tree
#
# Хочешь создать классный уровень? Сделай это при помощи build level или добавь уровень друга при помощи import level
#
# Команда show commands покажет все доступные инструкции. Там есть очень полезные, например undo и reset
#
# А пока просто начни игру при помощи levels...
